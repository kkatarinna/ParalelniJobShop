#include <algorithm>
#include <execution>
#include <fstream>
#include <iostream>
#include <vector>

#include "common/tests.hpp"
#include "parallel/merge.hpp"
#include "parallel/sort.hpp"
#include "serial/merge.hpp"
#include "parallel/sort.hpp"

namespace test {
    void correctness(size_t sz) {
    }

    void performance(size_t sz) {
    }

    void trivial() {
    }
}
